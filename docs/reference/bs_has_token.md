# Check if authenticated with Brightspace

Check if authenticated with Brightspace

## Usage

``` r
bs_has_token()
```

## Value

Logical; `TRUE` if a token is available.

## Examples

``` r
bs_has_token()
#> [1] FALSE
```
