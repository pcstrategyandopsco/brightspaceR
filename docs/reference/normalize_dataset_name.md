# Normalize a dataset name to a clean identifier

Converts a dataset name like "Users" or "User Enrollments" to a
snake_case key like "users" or "user_enrollments".

## Usage

``` r
normalize_dataset_name(name)
```

## Arguments

- name:

  Dataset name.

## Value

Character string.
