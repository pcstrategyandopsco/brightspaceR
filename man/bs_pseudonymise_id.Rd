% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/privacy.R
\name{bs_pseudonymise_id}
\alias{bs_pseudonymise_id}
\title{Pseudonymise a vector of IDs}
\usage{
bs_pseudonymise_id(values, key)
}
\arguments{
\item{values}{A vector of IDs (integer, numeric, or character). \code{NA} values
are preserved.}

\item{key}{A raw vector used as the HMAC key. Generate one per session with
\code{openssl::rand_bytes(32)}. Required — there is no default, to force
deliberate key management.}
}
\value{
A character vector the same length as \code{values}, with non-\code{NA}
entries replaced by \code{usr_} plus 8 hex characters.
}
\description{
Replaces each value with an HMAC-SHA256 pseudonym of the form \code{usr_a3f2b1c8}.
The same value + key always produces the same pseudonym, so joins and
grouping work correctly within a session.
}
\examples{
key <- openssl::rand_bytes(32)
bs_pseudonymise_id(c(1, 2, NA, 3), key = key)
}
