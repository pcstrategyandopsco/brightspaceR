% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/privacy.R
\name{bs_pseudonymise_df}
\alias{bs_pseudonymise_df}
\title{Pseudonymise person-referencing ID columns in a data frame}
\usage{
bs_pseudonymise_df(df, dataset_name, key, columns = NULL)
}
\arguments{
\item{df}{A data frame (typically from \code{\link[=bs_get_dataset]{bs_get_dataset()}}).}

\item{dataset_name}{Character string identifying the BDS dataset (e.g.
\code{"Users"}, \code{"Grade Results"}). Used to look up which columns contain
person-referencing IDs.}

\item{key}{A raw vector used as the HMAC key (passed to
\code{\link[=bs_pseudonymise_id]{bs_pseudonymise_id()}}).}

\item{columns}{Character vector of column names to pseudonymise. If \code{NULL}
(the default), the built-in registry is used based on \code{dataset_name}. If
\code{dataset_name} is not in the registry and \code{columns} is \code{NULL}, the data
frame is returned unchanged.}
}
\value{
The input data frame with person-referencing columns replaced by
pseudonyms.
}
\description{
Applies \code{\link[=bs_pseudonymise_id]{bs_pseudonymise_id()}} to the person-referencing columns for a
known Brightspace Data Set. Structural IDs (OrgUnitId, GradeObjectId, etc.)
are left untouched.
}
\examples{
key <- openssl::rand_bytes(32)
df <- data.frame(UserId = c(1L, 2L), OrgUnitId = c(10L, 20L))
bs_pseudonymise_df(df, "Users", key = key)
}
