% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{bs_config}
\alias{bs_config}
\title{Read Brightspace credentials from a config file}
\usage{
bs_config(
  file = "config.yml",
  profile = Sys.getenv("R_CONFIG_ACTIVE", "default")
)
}
\arguments{
\item{file}{Path to the YAML config file. Defaults to \code{"config.yml"} in the
working directory.}

\item{profile}{Configuration profile to use. Defaults to the
\code{R_CONFIG_ACTIVE} environment variable, or \code{"default"} if unset.}
}
\value{
A named list with elements \code{client_id}, \code{client_secret},
\code{instance_url}, \code{redirect_uri}, and \code{scope}, or \code{NULL} if the file does
not exist or the \code{brightspace} key is missing.
}
\description{
Reads Brightspace OAuth2 credentials from a YAML configuration file using
the \pkg{config} package. The function looks for a \code{brightspace} key in the
config file and returns the credentials as a named list.
}
\examples{
\donttest{
if (bs_has_token()) {
# Read from default config.yml
cfg <- bs_config()
cfg$client_id

# Read from a custom file and profile
cfg <- bs_config(file = "my-config.yml", profile = "production")
}
}
}
