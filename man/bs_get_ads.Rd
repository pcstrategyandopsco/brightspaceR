% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ads.R
\name{bs_get_ads}
\alias{bs_get_ads}
\title{Get an ADS dataset by name (convenience wrapper)}
\usage{
bs_get_ads(name, filters = list(), poll_interval = 5, timeout = 300)
}
\arguments{
\item{name}{Dataset name (case-insensitive). For example,
\code{"Learner Usage"}.}

\item{filters}{Optional filter list from \code{\link[=bs_ads_filter]{bs_ads_filter()}}.}

\item{poll_interval}{Seconds between status checks. Default 5.}

\item{timeout}{Maximum seconds to wait for completion. Default 300.}
}
\value{
A tibble of the dataset contents.
}
\description{
High-level function that finds the dataset by name, creates an export job,
polls until complete, downloads the result, and returns a tidy tibble.
Intended for interactive use.
}
\examples{
\donttest{
if (bs_has_token()) {
usage <- bs_get_ads("Learner Usage")
usage <- bs_get_ads("Learner Usage",
  filters = bs_ads_filter(start_date = "2024-01-01"))
}
}
}
