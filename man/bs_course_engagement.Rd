% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics.R
\name{bs_course_engagement}
\alias{bs_course_engagement}
\title{Calculate per-user per-course engagement metrics}
\usage{
bs_course_engagement(learner_usage, tz = NULL)
}
\arguments{
\item{learner_usage}{A tibble from the Learner Usage ADS.}

\item{tz}{Timezone for date conversion. Defaults to \code{\link[=bs_get_timezone]{bs_get_timezone()}}.}
}
\value{
A tibble with all learner_usage identity columns plus computed
metrics: \code{progress_pct}, \code{days_since_visit}.
}
\description{
Computes engagement metrics from Learner Usage ADS data including progress
percentage, days since last visit, and passes through all raw activity counts.
No composite score is computed here — use \code{\link[=bs_engagement_score]{bs_engagement_score()}} to add one.
}
\examples{
\donttest{
if (bs_has_token()) {
usage <- bs_get_ads("Learner Usage")
engagement <- bs_course_engagement(usage)
}
}
}
