% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics.R
\name{bs_engagement_score}
\alias{bs_engagement_score}
\title{Add a composite engagement score}
\usage{
bs_engagement_score(df, weights = list())
}
\arguments{
\item{df}{A tibble with activity count columns.}

\item{weights}{A named list of column-weight pairs to override defaults.
Defaults: \code{login_count = 1}, \code{quiz_completed = 3},
\code{assignment_completed = 5}, \code{discussion_posts_created = 2}.}
}
\value{
The input tibble with an \code{engagement_score} column appended.
}
\description{
Adds a weighted composite \code{engagement_score} column to any tibble that has
raw activity count columns. Default weights reflect relative effort/depth
(login is passive, assignment is active). Users should override for their
context.
}
\examples{
\donttest{
if (bs_has_token()) {
usage <- bs_get_ads("Learner Usage")
scored <- bs_engagement_score(usage)
scored <- bs_engagement_score(usage, weights = list(login_count = 2))
}
}
}
