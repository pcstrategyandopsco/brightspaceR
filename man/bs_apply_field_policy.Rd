% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/privacy.R
\name{bs_apply_field_policy}
\alias{bs_apply_field_policy}
\title{Apply a PII field policy to a data frame}
\usage{
bs_apply_field_policy(df, dataset_name, policy = NULL)
}
\arguments{
\item{df}{A data frame (typically from \code{\link[=bs_get_dataset]{bs_get_dataset()}}).}

\item{dataset_name}{Character string identifying the BDS dataset.}

\item{policy}{A named list representing the field policy (as returned by
\code{\link[yaml:read_yaml]{yaml::read_yaml()}}). If \code{NULL} (the default), loads the bundled
\code{field_policy.yml} shipped with the package.}
}
\value{
The input data frame with the field policy applied.
}
\description{
Filters or redacts columns based on a YAML-driven field policy. This is the
same logic the MCP server uses to strip PII before data reaches the AI
model.
}
\details{
The policy supports three modes per dataset:
\describe{
\item{\code{allow}}{Only the listed fields are kept; all others are dropped.}
\item{\code{redact}}{The listed fields have their values replaced with
\code{"[REDACTED]"}; all other fields pass through.}
\item{\code{all}}{All columns pass through unchanged.}
}

If \code{dataset_name} is not found in the policy, the data frame is returned
unchanged.
}
\examples{
df <- data.frame(
  UserId = 1L, FirstName = "Jane", Organization = "Org1",
  stringsAsFactors = FALSE
)
bs_apply_field_policy(df, "Users")
}
