% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics.R
\name{bs_filter_test_users}
\alias{bs_filter_test_users}
\title{Filter test users from a dataset}
\usage{
bs_filter_test_users(
  df,
  min_id_length = 30,
  exclusion_list = NULL,
  id_col = "org_defined_id"
)
}
\arguments{
\item{df}{A tibble containing user data.}

\item{min_id_length}{Minimum character length of a real user ID (default 30).
IDs shorter than this are assumed to be test accounts.}

\item{exclusion_list}{Optional character vector of specific IDs to exclude.}

\item{id_col}{Name of the column containing user IDs (default
\code{"org_defined_id"}).}
}
\value{
A filtered tibble with test users removed.
}
\description{
Removes test/system accounts using a two-layer filter: ID string length and
an optional exclusion list. This eliminates the repeated boilerplate of
removing test users before analysis.
}
\examples{
\donttest{
if (bs_has_token()) {
users <- bs_get_dataset("Users")
real_users <- bs_filter_test_users(users)
real_users <- bs_filter_test_users(users, exclusion_list = c("testuser01"))
}
}
}
