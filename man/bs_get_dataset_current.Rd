% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets.R
\name{bs_get_dataset_current}
\alias{bs_get_dataset_current}
\title{Get current dataset by merging full and differential extracts}
\usage{
bs_get_dataset_current(name, keep_deleted = FALSE)
}
\arguments{
\item{name}{Dataset name (case-insensitive partial match). For example,
\code{"Users"}, \code{"Grade Results"}, \code{"Org Units"}.}

\item{keep_deleted}{If \code{FALSE} (default), rows marked as deleted in
differential extracts are removed from the final result.}
}
\value{
A tibble of the merged dataset contents. The tibble carries a
\code{"bds_manifest"} attribute with the extract breakdown (full, each diff,
and merged total with row counts and download status). Retrieve it with
\code{\link[=bs_diff_manifest]{bs_diff_manifest()}}.
}
\description{
Downloads the latest full extract and all subsequent differential extracts
for a dataset, then merges them to produce a current-as-of-today tibble.
}
\examples{
\donttest{
if (bs_has_token()) {
users <- bs_get_dataset_current("Users")
bs_diff_manifest(users)
}
}
}
